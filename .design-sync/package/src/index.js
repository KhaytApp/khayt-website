/* Khayt's design system is foundations only: colour, type and spacing.
   There are no components to export — the website renders HTML strings and
   the desktop app is vanilla DOM, so there is no React component library to
   convert. The look lives entirely in the token files under styles/, which
   are derived from the site's own styles.css.

   This entry exists because the converter bundles a package entry; a
   tokens-only design system bundles to an empty body, which is correct. */
export {};
